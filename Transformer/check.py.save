import numpy as np, os

features_dir = "data/features"
total_frames = 0

for f in sorted(os.listdir(features_dir)):
    if f.endswith(".npy"):
        arr = np.load(os.path.join(features_dir, f))

for f in sorted(os.listdir(features_dir)):
    if f.endswith(".npy"):
        arr = np.load(os.path.join(features_dir, f))
        total_frames += arr.shape[0
print(f"총 샘플(프레임) 수: {total_frames}")
