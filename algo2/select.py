# select Pseudocode

"""
def select(S, k):
    # choose a pivot s uniformly at random.
    for e in range(len(S)):
        if e > s:
            Sr.append(S.pop(s))
        if e < s:
            Sl.append(S.pop(s))
    
    if len(Sl) < k - 1:
        return s
    elif len(Sl) >= k:
        return select(Sl, k)
    elif len(Sl) < k:
        return select(Sr, k - len(Sl) - 1)"""
        
# Quick Pseudocode

"""
"""